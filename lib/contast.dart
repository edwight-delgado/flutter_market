import 'package:flutter/material.dart';

const backgroundColor = Color(0xFFF6F5F2);
const cardBarHeight = 100.0;
const panelTransition = Duration(milliseconds: 500);
const yellowColor = Color(0xFFFF4C459);
